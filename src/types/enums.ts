export enum EAlertTypes {
  Success = "success",
  Error = "error",
  Info = "info",
  Warning = "warning",
}

export enum ETaskTypes {
  Todo = "todo",
  Doing = "doing",
  Done = "done",
}

export enum ELocalStorageKeys {
  TEMPCOLUMNS = "temp-columns",
  COLUMNS = "columns",
  USER = "user",
}
